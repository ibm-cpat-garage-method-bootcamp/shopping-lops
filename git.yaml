apiVersion: tekton.dev/v1alpha1
kind: PipelineResource
metadata:
  name: shopping-lops-git
spec:
  type: git
  params:
    - name: revision
      value: master
    - name: url
      value: https://github.com/ibm-cpat-garage-method-bootcamp/shopping-lops